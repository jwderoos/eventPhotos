{% extends 'admin/_base.html.twig' %}

{% block title %}Admin — {{ mode == 'new' ? 'New event' : 'Edit ' ~ event.name }}{% endblock %}

{% block admin_main %}
    <h1>{{ mode == 'new' ? 'New event' : 'Edit ' ~ event.name }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">{{ mode == 'new' ? 'Create' : 'Save' }}</button>
    <a href="{{ path('admin_event_index') }}">Cancel</a>
    {{ form_end(form) }}
{% endblock %}
