framework:
    http_client:
        scoped_clients:
            inference.client:
                base_uri: '%env(INFERENCE_SERVICE_URL)%'
                timeout: 300
                max_duration: 600
