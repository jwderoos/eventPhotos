framework:
    http_client:
        scoped_clients:
            inference.client:
                base_uri: '%env(INFERENCE_SERVICE_URL)%'
                timeout: 10
                max_duration: 60
